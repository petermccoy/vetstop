<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this just styles the window before setContent. -->
    <style name="Theme.VetStop" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
